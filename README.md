### Install

```bash
# NodeJS must be downloaded to run the commands below.

# In your root directory install backend dependencies
npm i

# In client folder, install frontend dependencies
cd client
npm i

cd ..
npm start

# Json server will run on port 5000, client will run on port 3000
